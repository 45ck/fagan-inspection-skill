policy:
  allow_implicit_invocation: false

interface:
  display_name: "Fagan Inspection"
  short_description: "Formal multi-domain inspection with a consolidated defect log and verification."
  default_prompt: "Use $fagan-inspection to review the current change set and produce a formal defect log."
